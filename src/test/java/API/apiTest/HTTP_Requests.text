🧪 1️⃣ GET All Spartans
@Test
void getAllSpartans() {

    given()
           .accept(ContentType.JSON)
    .when()
           .get("/api/spartans")
    .then()
            .statusCode(200)
            .contentType(ContentType.JSON);
}
-----------------------------------------------------------------
🧪 2️⃣ GET Spartan By ID
@Test
void getSpartanById() {

    given()
            .accept(ContentType.JSON)
            .pathParam("id", 3)
    .when()
            .get("/api/spartans/{id}")
    .then()
            .statusCode(200)
            .body("id", equalTo(3));
}
--------------------------------------------------------------------
🧪 3️⃣ POST Create Spartan
@Test
void createSpartan() {

    Spartan spartan = new Spartan("Nargiza", "Female", 1234567890L);

    given()
            .accept(ContentType.JSON)
            .contentType(ContentType.JSON)
            .body(spartan)
    .when()
            .post("/api/spartans")
    .then()
            .statusCode(201)
            .body("success", equalTo("A Spartan is Born!"))
            .body("data.name", equalTo("Nargiza"));
}
-----------------------------------------------------------------------------
🧪 4️⃣ PUT Update Spartan
@Test
void updateSpartan() {

    Spartan updated = new Spartan("UpdatedName", "Female", 9999999999L);

    given()
            .contentType(ContentType.JSON)
            .pathParam("id", 3)
            .body(updated)
    .when()
            .put("/api/spartans/{id}")
    .then()
            .statusCode(204);
}
-------------------------------------------------------------------------------
🧪 5️⃣ DELETE Spartan
@Test
void deleteSpartan() {

    given()
            .pathParam("id", 3)
    .when()
            .delete("/api/spartans/{id}")
    .then()
            .statusCode(204);
}
----------------------------------------------------------------------------------
🧪 6️⃣ Negative Test – Invalid ID
@Test
void getInvalidSpartan() {

    given()
            .accept(ContentType.JSON)
            .pathParam("id", 999999)
    .when()
            .get("/api/spartans/{id}")
    .then()
            .statusCode(404);
}